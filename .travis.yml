sudo: required

language: node_js

node_js:
  # - "node"
  - "8"

services:
  - docker

before_install:
- docker pull dgraph/dgraph:v0.9.1
- docker run bash -c "dgraph zero --port_offset -2000 & dgraph server --memory_mb 1028 --zero localhost:5080"
